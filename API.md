# Справочник функций и процедур (API)

В данном справочнике приведено полное техническое описание всех исходных функций и процедур интеграционного комплекта для работы с СУБД ClickHouse.

---

### Функция `ПолучитьНастройкиИзРегистра`
Выполняет чтение параметров авторизации и подключения к СУБД ClickHouse из регистра сведений.

```bsl
Функция ПолучитьНастройкиИзРегистра() Экспорт
	Результат = Новый Структура("АдресСервера, Пользователь, Пароль, ПутьККомпоненте", "", "", "", "");
	
	Запрос = Новый Запрос;
	Запрос.Текст=
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	КХ_НастройкиПодключения.АдресСервера КАК АдресСервера,
		|	КХ_НастройкиПодключения.Пользователь КАК Пользователь,
		|	КХ_НастройкиПодключения.Пароль КАК Пароль,
		|	КХ_НастройкиПодключения.ПутьККомпоненте КАК ПутьККомпоненте
		|ИЗ
		|	РегистрСведений.КХ_НастройкиПодключения КАК КХ_НастройкиПодключения";
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Результат, Выборка);
	КонецЕсли;
	Возврат Результат;
КонецФункции
```

---

### Функция `ОчиститьСтрокуОтНулей`
Очищает входящую текстовую строку от служебных нулевых символов (`Символ(0)`), которые могут возвращаться внешними библиотеками.

```bsl
Функция ОчиститьСтрокуОтНулей(Знач ВходящаяСтрока) Экспорт
	Если Не ЗначениеЗаполнено(ВходящаяСтрока) Тогда
		Возврат ВходящаяСтрока;
	КонецЕсли;

	Если Прав(ВходящаяСтрока, 1) = Символ(0) Тогда
		Возврат Лев(ВходящаяСтрока, СтрДлина(ВходящаяСтрока) - 1);
	КонецЕсли;
	
	Возврат ВходящаяСтрока;
КонецФункции
```

---

### Функция `РаспарситьОтветClickHouse`
Преобразует текстовый ответ от сервера ClickHouse в объект 1С (Структура или Соответствие), предварительно очищая строку от нулевых байтов.

```bsl
Функция РаспарситьОтветClickHouse(Знач СыройОтвет) Экспорт
	
	ЧистыйJSON = ОчиститьСтрокуОтНулей(СыройОтвет);
	
	ЧтениеJSON = Новый ЧтениеJSON;
	ЧтениеJSON.УстановитьСтроку(ЧистыйJSON);
	Результат = ПрочитатьJSON(ЧтениеJSON);
	ЧтениеJSON.Закрыть();
	
	Возврат Результат;
	
КонецФункции
```

---

### Функция `ПолучитьПодключениеClickHouse`
Выполняет динамическое подключение внешней компоненты (из макета или папки на диске) и инициализирует авторизованную сессию с ClickHouse.

```bsl
Функция ПолучитьПодключениеClickHouse() Экспорт
	Настройки = КХ_ИнтеграцияСлужебный.ПолучитьНастройкиИзРегистра();
	
	Попытка
		ИмяСборки = "CH_Comp";
		Если ПустаяСтрока(Настройки.ПутьККомпоненте) Тогда
			ИсточникЗагрузки = "ОбщийМакет.КХ_ВК";
			ИмяТипаОбъекта   = "AddIn." + ИмяСборки + ".ClickHouse"; // Для макета пробуем стандартный синтаксис
		Иначе
			ИсточникЗагрузки = Настройки.ПутьККомпоненте;
			ИмяТипаОбъекта   = "AddIn." + ИмяСборки + ".ClickHouse"; // Для dll на диске
		КонецЕсли;
		
		Подключено = ПодключитьВнешнююКомпоненту(ИсточникЗагрузки, ИмяСборки, ТипВнешнейКомпоненты.Native);
		Если НЕ Подключено Тогда
			ВызватьИсключение "Платформа 1С вернула Ложь при загрузке бинарника.";
		КонецЕсли;
		
		ВК_Объект = Новый(ИмяТипаОбъекта);
		
		Если Не ВК_Объект.Подключиться(Настройки.АдресСервера, Настройки.Пользователь, Настройки.Пароль) Тогда
			ВызватьИсключение "Компонента Rust отказала в авторизации сессии ClickHouse.";
		КонецЕсли;
		
		Возврат ВК_Объект;
	Исключение
		ЗаписьЖурналаРегистрации("ClickHouse.Ядро", УровеньЖурналаРегистрации.Ошибка,,, 
			"Критическая ошибка инициализации ядра ClickHouse: " + ОписаниеОшибки());
		Возврат Неопределено;
	КонецПопытки;
КонецФункции
```

---

### Функция `НачатьЗамер`
Создает базовую структуру метрики и фиксирует текущее универсальное время для расчета длительности выполнения операции.

```bsl
Функция НачатьЗамер(Знач ТипСобытия, Знач ИмяСобытия) Экспорт
	Метрика = Новый Структура();
	Метрика.Вставить("ТипСобытия",  ТипСобытия);
	Метрика.Вставить("ИмяСобытия",  ИмяСобытия);
	Метрика.Вставить("ВремяСтарта", ТекущаяУниверсальнаяДатаВМиллисекундах());
	Метрика.Вставить("Контекст",    Новый Структура);
	
	Возврат Метрика;
КонецФункции
```

---

### Процедура `ФиксироватьМетрику`
Рассчитывает время выполнения, сериализует контекст в JSON и передает собранную метрику во внешнюю компоненту для отправки в СУБД.

```bsl
Процедура ФиксироватьМетрику(Метрика, Знач Статус = "Succes") Экспорт
	ДлительностьМС = ТекущаяУниверсальнаяДатаВМиллисекундах() - Метрика.ВремяСтарта;
	ТекущийПользователь = ПользователиИнформационнойБазы.ТекущийПользователь().Имя;
		
	ЗаписьJSON = Новый ЗаписьJSON;
	ЗаписьJSON.УстановитьСтроку();
	ЗаписатьJSON(ЗаписьJSON, Метрика.Контекст);
	СтрокаКонтекстаJSON = ЗаписьJSON.Закрыть();
	
	ВК = КХ_ЯдроПовИсп.ПолучитьПодключениеClickHouse();
	
	Если ВК <> Неопределено Тогда
		Попытка
			ВК.ЛогМетрики(
				Метрика.ТипСобытия,
				Метрика.ИмяСобытия,
				ДлительностьМС,
				Статус,
				ТекущийПользователь,
				СтрокаКонтекстаJSON
			);
		Исключение
			ЗаписьЖурналаРегистрации("ClickHouse.Метрики", УровеньЖурналаРегистрации.Предупреждение,,, 
				"Сбой отправки метрики: " + ОписаниеОшибки());
		КонецПопытки;
	КонецЕсли;
КонецПроцедуры
```

---

### Процедура `ВыполнитьПакетнуюВставку`
Передает пакет строк в формате TSV во внешнюю компоненту для массовой вставки в целевую таблицу ClickHouse.

```bsl
Процедура ВыполнитьПакетнуюВставку(Знач ИмяТаблицы, Знач СтрокаДанныхTSV, Знач ВыполнятьПроверкуТаблицы = Ложь) Экспорт
	ВК = КХ_ЯдроПовИсп.ПолучитьПодключениеClickHouse();
	Если ВК = Неопределено Тогда
		ВызватьИсключение "Не удалось получить активное подключение к ядру ClickHouse.";
	КонецЕсли;
	
	Попытка
		Результат = ВК.ПакетнаяВставка(ИмяТаблицы, СтрокаДанныхTSV, ВыполнятьПроверкуТаблицы);
		ЧистыйРезультат = КХ_ИнтеграцияСлужебный.ОчиститьСтрокуОтНулей(Результат);
		Если ЧистыйРезультат <> "OK" Тогда
			ВызватьИсключение ЧистыйРезультат;
		КонецЕсли;
	Исключение
		ЗаписьЖурналаРегистрации("ClickHouse.Данные", УровеньЖурналаРегистрации.Ошибка,,, 
			"Ошибка пакетной вставки в таблицу '" + ИмяТаблицы + "': " + ОписаниеОшибки());
		ВызватьИсключение ОписаниеОшибки();
	КонецПопытки;
КонецПроцедуры
```

---

### Функция `ВыполнитьЗапрос`
Отправляет произвольный SQL-запрос в ClickHouse через внешнюю компоненту и возвращает текстовый ответ.

```bsl
Функция ВыполнитьЗапрос(Знач ТекстЗапросаSQL) Экспорт
	
	ВК = КХ_ЯдроПовИсп.ПолучитьПодключениеClickHouse();
	Если ВК = Неопределено Тогда
		Возврат "Ошибка: Нет подключения к ClickHouse";
	КонецЕсли;
	
	Попытка
		СыройОтвет = ВК.ВыполнитьКоманду(ТекстЗапросаSQL);
		Возврат КХ_ИнтеграцияСлужебный.ОчиститьСтрокуОтНулей(СыройОтвет);
	Исключение
		Возврат "Ошибка выполнения запроса: " + ОписаниеОшибки();
	КонецПопытки;
	
КонецФункции
```

---

### Функция `КонвертироватьТЗвTSV`
Конвертирует данные `ТаблицыЗначений` в формат TSV (разделенный табуляциями текст) с использованием временного файла на диске для последующего импорта.

```bsl
Функция КонвертироватьТЗвTSV(ТаблицаДанных) Экспорт
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла("tsv");
	ЗаписьТекста = Новый ЗаписьТекста(ИмяВременногоФайла, КодировкаТекста.UTF8);
	
	МассивКолонок = Новый Массив;
	Для Каждого Колонка Из ТаблицаДанных.Колонки Цикл
		МассивКолонок.Добавить(Колонка.Имя);
	КонецЦикла;
	СтрокаЗаголовков = СтрСоединить(МассивКолонок, Символы.Таб);
	ЗаписьТекста.ЗаписатьСтроку(СтрокаЗаголовков);
	
	КоличествоКолонок = МассивКолонок.Количество();
	Для Каждого СтрокаТЗ Из ТаблицаДанных Цикл
		
		МассивЗначений = Новый Массив;
		Для Индекс = 0 По КоличествоКолонок - 1 Цикл
			Значение = СтрокаТЗ[Индекс];

			Если ТипЗнч(Значение) = Тип("Булево") Тогда
				ТекстЗначения = ?(Значение, "true", "false");
			ИначеЕсли ТипЗнч(Значение) = Тип("Дата") Тогда
				ТекстЗначения = Формат(Значение, "ДФ='yyyy-MM-dd HH:mm:ss'");
			ИначеЕсли Значение = Неопределено Или Значение = Null Тогда
				ТекстЗначения = "NULL";
			Иначе
				ТекстЗначения = СтрЗаменить(Строка(Значение), Символы.Таб, " ");
			КонецЕсли;
			
			МассивЗначений.Добавить(ТекстЗначения);
		КонецЦикла;
		
		ЗаписьТекста.ЗаписатьСтроку(СтрСоединить(МассивЗначений, Символы.Таб));
		
	КонецЦикла;
	ЗаписьТекста.Закрыть();
	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.Прочитать(ИмяВременногоФайла, КодировкаТекста.UTF8);
	СтрокаTSV = ТекстовыйДокумент.ПолучитьТекст();
	Попытка УдалитьФайлы(ИмяВременногоФайла); Исключение КонецПопытки;
	
	Возврат СтрокаTSV;
КонецФункции
```

---

### Процедура `ВкачатьТаблицуЗначений`
Точка входа для загрузки `ТаблицыЗначений` в базу ClickHouse. Запускает процесс подготовки TSV-строки и выполняет операцию вставки пакета.

```bsl
Процедура ВкачатьТаблицуЗначений(Знач ИмяТаблицыClickHouse, ТаблицаДанных, Знач ВыполнятьПроверку = Ложь) Экспорт
	Если ТаблицаДанных.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	СтрокаДанныхTSV = КонвертироватьТЗвTSV(ТаблицаДанных);
	
	ВыполнитьПакетнуюВставку(ИмяТаблицыClickHouse, СтрокаДанныхTSV, ВыполнятьПроверку);
КонецПроцедуры
```
